# Contributing

1. Install dependencies with `pnpm install`.
2. Run `pnpm run check` before submitting changes.
3. Keep VS Code and DOM dependencies outside `src/domain/`.
4. Add catalog entries in both English and Spanish.
5. Add pure unit tests for domain or command-building changes.
