# Release checklist

1. Update the version and changelog.
2. Run `pnpm run check`.
3. Run `pnpm run package`.
4. Install the generated VSIX in an Extension Development Host and verify add, edit, move, remove, and connect flows.
